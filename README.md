# Stiply openapi client generated by Maven

We're phasing issues generating Stiply client code.

Obtained contract from https://app.stiply.nl/v2/.metadata/openapi.json

Run with

    ./mvnw clean && ./mvnw package
